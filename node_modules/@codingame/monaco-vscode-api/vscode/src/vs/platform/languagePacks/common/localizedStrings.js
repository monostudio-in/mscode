
import { localize } from '../../../nls.js';

const open = ( localize(1998, "open"));
const close = ( localize(1999, "close"));
const find = ( localize(2000, "find"));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
