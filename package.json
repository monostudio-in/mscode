{
  "name": "mscode-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=mscode-app",
    "build": "npm run build --workspace=mscode-app",
    "build:android": "npm run build:android --workspace=mscode-app",
    "cli": "npm run start --workspace=@mscode/msce",
    "docs": "npm run docs:dev --workspace=mscode-app"
  },
  "devDependencies": {
    "dts-bundle-generator": "^9.5.1",
    "patch-package": "^8.0.1"
  },
  "dependencies": {
    "@monostudio/msce": "^1.0.1"
  }
}
